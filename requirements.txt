# KonnexCore — pinned Python dependencies.
# Source of truth: build spec Section 4. Versions are pinned exactly so
# grant reviewers get bit-identical behavior on `pip install -r requirements.txt`.

# --- Core framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Math / sim ---
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
filterpy==1.4.5
# pybullet==3.2.6  # optional: sim engine only

# --- Crypto ---
cryptography>=43.0.1
pynacl==1.5.0

# --- CLI & UX ---
click==8.1.7
rich==13.8.1

# --- HTTP / data ---
httpx==0.27.2
orjson==3.10.7

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
# Property-based testing (per the property-based-testing skill); pinned to
# the latest patch on the 6.112.x line at time of writing.
hypothesis==6.112.1

# --- Optional: LLM comparison only ---
openai==1.51.0
# opencv-python==4.10.0.84  # optional: LLM compare only

# --- Dev ---
black==24.8.0
ruff==0.6.8
mypy==1.11.2
